"use client";

import { useEffect, useState } from "react";
import {
  Card,
  CardContent,
  CardHeader,
  CardTitle,
} from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { Button } from "@/components/ui/button";
import { Textarea } from "@/components/ui/textarea";
import { Badge } from "@/components/ui/badge";
// import JoditEditor from "./EditorWrapper";
import JoditEditor from "jodit-react";
import { Calendar, User, Heart, Share2 } from "lucide-react";
import { Avatar, AvatarFallback, AvatarImage } from "@/components/ui/avatar";
import { Header } from "@/components/header";
import { Footer } from "@/components/footer";
import Image from "next/image";
import Link from "next/link";

// Dummy content object – you will replace this with dynamic props or fetched data
const content = {
  article: {
    date: "",
    author: "",
    title: "",
    intro: "",
    targetAudience: [],
    quote: "",
    whyChooseTitle: "",
    whyChooseText1: "",
    whyChooseText2: "",
    badges: [],
  },
  relatedPosts: {
    title: "",
    post1: { title: "", date: "", description: "", link: "" },
    post2: { title: "", date: "", description: "", link: "" },
  },
  sidebar: {
    about: { title: "", description: "" },
    popularPosts: { title: "", posts: [] },
    categories: { title: "", items: [] },
    tags: { title: "", items: [] },
  },
};

interface Blog {
  title: string;
  intro?: string;
  description: string;
  date: string;
  readTime?: string;
  category: string;
  author?: string;
  avatar?: string;
  quote?: string;
  whyChooseTitle?: string;
  whyChooseText1?: string;
  whyChooseText2?: string;
  targetAudience: string[];
  badges: string[];
}

export default function AdminBlog() {
  const [formData, setFormData] = useState<Blog>({
    title: "",
    intro: "",
    description: "",
    date: "",
    readTime: "",
    category: "",
    author: "",
    avatar: "",
    quote: "",
    whyChooseTitle: "",
    whyChooseText1: "",
    whyChooseText2: "",
    targetAudience: [],
    badges: [],
  });

  const handleChange = (
    e: React.ChangeEvent<HTMLInputElement | HTMLTextAreaElement>
  ) => {
    setFormData({ ...formData, [e.target.name]: e.target.value });
  };

  const handleDescriptionChange = (value: string) => {
    setFormData({ ...formData, description: value });
  };

  const handleEditorChange = (field: string, value: string) => {
    setFormData({ ...formData, [field]: value });
  };

  const handleDynamicChange = (
    index: number,
    field: "targetAudience" | "badges",
    value: string
  ) => {
    const updated = [...formData[field]];
    updated[index] = value;
    setFormData({ ...formData, [field]: updated });
  };

  const addField = (field: "targetAudience" | "badges") => {
    setFormData({ ...formData, [field]: [...formData[field], ""] });
  };

  console.log("formData", formData);

  return (
    <div className="min-h-screen bg-gradient-to-br from-purple-900 via-violet-800 to-purple-900">
        <Header/>
      <div className="relative max-w-7xl mx-auto px-4 py-8" >
        <div className="flex flex-col lg:flex-row gap-8 pt-12">
          {/* Left - Form */}
          <div className="lg:w-1/2">
            <Card className="bg-white/10 backdrop-blur-sm border border-white/20">
              <CardHeader>
                <CardTitle className="text-2xl text-white">Create Blog Post</CardTitle>
              </CardHeader>
              <CardContent className="space-y-6">
                <div className="space-y-2">
                  <label className="text-sm font-medium text-purple-200">Title</label>
                  <Input
                    type="text"
                    name="title"
                    placeholder="Enter blog title"
                    value={formData.title}
                    onChange={handleChange}
                    className="bg-white/5 border-white/20 text-white placeholder:text-purple-300"
                  />
                </div>

                <div className="space-y-2">
                  <label className="text-sm font-medium text-purple-200">Introduction</label>
                  <Textarea
                    name="intro"
                    placeholder="Write a brief introduction"
                    value={formData.intro}
                    onChange={handleChange}
                    className="bg-white/5 border-white/20 text-white placeholder:text-purple-300"
                    rows={4}
                  />
                </div>

                <div className="space-y-2">
                  <label className="text-sm font-medium text-purple-200">Main Content</label>
                  <JoditEditor
                    value={formData.description}
                    onChange={handleDescriptionChange}
                  />
                </div>

                <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
                  <div className="space-y-2">
                    <label className="text-sm font-medium text-purple-200">Date</label>
                    <Input
                      type="text"
                      name="date"
                      placeholder="e.g., Aug 2, 2025"
                      value={formData.date}
                      onChange={handleChange}
                      className="bg-white/5 border-white/20 text-white placeholder:text-purple-300"
                    />
                  </div>
                  <div className="space-y-2">
                    <label className="text-sm font-medium text-purple-200">Read Time</label>
                    <Input
                      type="text"
                      name="readTime"
                      placeholder="e.g., 5 min read"
                      value={formData.readTime}
                      onChange={handleChange}
                      className="bg-white/5 border-white/20 text-white placeholder:text-purple-300"
                    />
                  </div>
                </div>

                <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
                  <div className="space-y-2">
                    <label className="text-sm font-medium text-purple-200">Category</label>
                    <Input
                      type="text"
                      name="category"
                      placeholder="Enter category"
                      value={formData.category}
                      onChange={handleChange}
                      className="bg-white/5 border-white/20 text-white placeholder:text-purple-300"
                    />
                  </div>
                  <div className="space-y-2">
                    <label className="text-sm font-medium text-purple-200">Author</label>
                    <Input
                      type="text"
                      name="author"
                      placeholder="Author name"
                      value={formData.author}
                      onChange={handleChange}
                      className="bg-white/5 border-white/20 text-white placeholder:text-purple-300"
                    />
                  </div>
                </div>

                <div className="space-y-2">
                  <label className="text-sm font-medium text-purple-200">Author Avatar URL</label>
                  <Input
                    type="text"
                    name="avatar"
                    placeholder="Enter avatar URL"
                    value={formData.avatar}
                    onChange={handleChange}
                    className="bg-white/5 border-white/20 text-white placeholder:text-purple-300"
                  />
                </div>

                <div className="space-y-2">
                  <label className="text-sm font-medium text-purple-200">Quote</label>
                  <Input
                    type="text"
                    name="quote"
                    placeholder="Enter quote"
                    value={formData.quote}
                    onChange={handleChange}
                    className="bg-white/5 border-white/20 text-white placeholder:text-purple-300"
                  />
                </div>

                <div className="space-y-2">
                  <label className="text-sm font-medium text-purple-200">Why Choose Title</label>
                  <Input
                    type="text"
                    name="whyChooseTitle"
                    placeholder="Why Choose section title"
                    value={formData.whyChooseTitle}
                    onChange={handleChange}
                    className="bg-white/5 border-white/20 text-white placeholder:text-purple-300"
                  />
                </div>

                <div className="space-y-2">
                  <label className="text-sm font-medium text-purple-200">Why Choose Text 1</label>
                  <JoditEditor
                    value={formData.whyChooseText1 || ""}
                    onChange={(val) => handleEditorChange("whyChooseText1", val)}
                  />
                </div>

                <div className="space-y-2">
                  <label className="text-sm font-medium text-purple-200">Why Choose Text 2</label>
                  <JoditEditor
                    value={formData.whyChooseText2 || ""}
                    onChange={(val) => handleEditorChange("whyChooseText2", val)}
                  />
                </div>

                <div className="space-y-2">
                  <label className="text-sm font-medium text-purple-200">Target Audience</label>
                  {formData.targetAudience.map((item, index) => (
                    <Textarea
                      key={index}
                      placeholder={`Audience point ${index + 1}`}
                      value={item}
                      onChange={(e) =>
                        handleDynamicChange(index, "targetAudience", e.target.value)
                      }
                      className="bg-white/5 border-white/20 text-white placeholder:text-purple-300 mb-2"
                      rows={3}
                    />
                  ))}
                  <Button
                    type="button"
                    onClick={() => addField("targetAudience")}
                    className="bg-cyan-400/20 hover:bg-cyan-400/30 text-cyan-400"
                  >
                    + Add Audience Point
                  </Button>
                </div>

                <div className="space-y-2">
                  <label className="text-sm font-medium text-purple-200">Badges</label>
                  {formData.badges.map((badge, index) => (
                    <Input
                      key={index}
                      placeholder={`Badge ${index + 1}`}
                      value={badge}
                      onChange={(e) =>
                        handleDynamicChange(index, "badges", e.target.value)
                      }
                      className="bg-white/5 border-white/20 text-white placeholder:text-purple-300 mb-2"
                    />
                  ))}
                  <Button
                    type="button"
                    onClick={() => addField("badges")}
                    className="bg-cyan-400/20 hover:bg-cyan-400/30 text-cyan-400"
                  >
                    + Add Badge
                  </Button>
                </div>

                <Button className="w-full bg-cyan-400 text-purple-900 hover:bg-cyan-300">
                  Submit Blog Post
                </Button>
              </CardContent>
            </Card>
          </div>

          {/* Right - Live Preview */}
          <div className="lg:w-1/2">
            <Card className="bg-white/10 backdrop-blur-sm border border-white/20">
              <CardHeader>
                <CardTitle className="text-2xl text-white">Live Preview</CardTitle>
              </CardHeader>
              <CardContent>
                <div className="relative">
                  {/* Background Pulses */}
                  <div className="absolute inset-0 overflow-hidden">
                    <div className="absolute -top-40 -right-40 w-80 h-80 bg-gradient-to-br from-purple-400/30 to-violet-600/20 rounded-full blur-3xl animate-pulse" />
                    <div className="absolute -bottom-40 -left-40 w-96 h-96 bg-gradient-to-tr from-violet-500/25 to-purple-400/15 rounded-full blur-3xl animate-pulse" style={{ animationDelay: "2s" }} />
                    <div className="absolute top-1/2 left-1/2 transform -translate-x-1/2 -translate-y-1/2 w-64 h-64 bg-gradient-to-bl from-purple-300/20 to-violet-700/10 rounded-full blur-2xl animate-pulse" style={{ animationDelay: "4s" }} />
                  </div>

                  {/* Grid Overlay */}
                  <div className="absolute inset-0 opacity-5" style={{ backgroundImage: `radial-gradient(circle at 1px 1px, rgba(255,255,255,0.4) 1px, transparent 0)`, backgroundSize: "40px 40px", animation: "gridMove 20s linear infinite" }} />

                  <div className="relative z-10">
                    <article className="bg-white/10 backdrop-blur-sm rounded-lg shadow-sm overflow-hidden border border-white/20">
                      <div className="relative h-64 md:h-80">
                        <Image src="/placeholder.svg?height=400&width=800" alt="Cover Image" fill className="object-cover" />
                        <div className="absolute inset-0 bg-gradient-to-t from-purple-900/60 to-transparent" />
                      </div>

                      <div className="p-6 md:p-8">
                        <div className="flex items-center gap-4 text-sm text-purple-200 mb-4">
                          <div className="flex items-center gap-1"><Calendar className="w-4 h-4 text-cyan-400" /><span>{formData.date}</span></div>
                          <div className="flex items-center gap-1"><User className="w-4 h-4 text-cyan-400" /><span>{formData.author}</span></div>
                        </div>

                        <h1 className="text-3xl md:text-4xl font-bold text-white mb-6">{formData.title}</h1>

                        <div className="prose prose-lg max-w-none text-purple-100">
                          <p className="mb-6">{formData.intro}</p>

                          <div className="grid grid-cols-2 gap-4 my-8">
                            {[...Array(2)].map((_, i) => (
                              <div key={i} className="relative h-48">
                                <Image src="/placeholder.svg?height=300&width=400" alt="Image" fill className="object-cover rounded-lg" />
                                <div className="absolute inset-0 bg-gradient-to-t from-purple-900/40 to-transparent rounded-lg" />
                              </div>
                            ))}
                          </div>

                          <div
                            className="leading-relaxed mb-8 animate-fade-in-up"
                            style={{ animationDelay: "0.9s" }}
                            dangerouslySetInnerHTML={{
                              __html: formData.description || "<p>Blog content preview...</p>",
                            }}
                          />
                          <blockquote className="border-l-4 border-cyan-400 pl-6 py-4 my-8 bg-white/10 backdrop-blur-sm rounded-r-lg">
                            <p className="text-white italic text-lg">{formData.quote}</p>
                          </blockquote>

                          <h2 className="text-2xl font-bold text-white mb-4">{formData.whyChooseTitle}</h2>
                          <div className="mb-6" dangerouslySetInnerHTML={{ __html: formData.whyChooseText1 || "" }} />
                          <div className="mb-8" dangerouslySetInnerHTML={{ __html: formData.whyChooseText2 || "" }} />
                        </div>

                        <div className="flex items-center justify-between pt-6 border-t border-white/20">
                          <div className="flex items-center gap-2">
                            {formData.badges.map((badge, i) => (
                              badge && (
                                <Badge key={i} variant="secondary" className="bg-purple-600/80 text-white hover:bg-purple-600/90">
                                  {badge}
                                </Badge>
                              )
                            ))}
                          </div>
                          <Button variant="ghost" size="sm" className="text-white hover:bg-white/10">
                            <Heart className="w-4 h-4 mr-1 text-cyan-400" />
                            24
                          </Button>
                        </div>
                      </div>
                    </article>

                    {/* Related Posts */}
                    {/* <div className="mt-12">
                      <h2 className="text-2xl font-bold text-white mb-6">{content.relatedPosts.title}</h2>
                      <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
                        {[content.relatedPosts.post1, content.relatedPosts.post2].map((post, i) => (
                          <Card key={i} className="overflow-hidden bg-white/10 backdrop-blur-sm border border-white/20">
                            <div className="relative h-48">
                              <Image src="/placeholder.svg?height=200&width=400" alt="Related" fill className="object-cover" />
                              <div className="absolute inset-0 bg-gradient-to-t from-purple-900/40 to-transparent" />
                            </div>
                            <CardContent className="p-4">
                              <h3 className="font-semibold text-white mb-2">{post.title}</h3>
                              <p className="text-sm text-purple-200 mb-2">{post.date}</p>
                              <p className="text-sm text-purple-200 mb-4">{post.description}</p>
                              <Link href={post.link} className="text-sm text-purple-300 underline hover:text-purple-100 transition">Learn More</Link>
                            </CardContent>
                          </Card>
                        ))}
                      </div>
                    </div> */}
                  </div>

                  {/* Sidebar */}
                  {/* <div className="mt-8 space-y-6">
                    <Card className="bg-white/10 backdrop-blur-sm border border-white/20">
                      <CardContent className="p-6">
                        <h3 className="font-semibold text-lg text-white mb-4">{content.sidebar.about.title}</h3>
                        <p className="text-sm text-purple-200 mb-4">{content.sidebar.about.description}</p>
                      </CardContent>
                    </Card>

                    <Card className="bg-white/10 backdrop-blur-sm border border-white/20">
                      <CardContent className="p-6">
                        <h3 className="font-semibold text-lg text-white mb-4">{content.sidebar.popularPosts.title}</h3>
                        <div className="space-y-4">
                          {content.sidebar.popularPosts.posts.map((post, i) => (
                            <Link key={i} href={post.link} className="block hover:opacity-90 transition">
                              <div className="flex gap-3">
                                <div className="relative w-16 h-16 flex-shrink-0">
                                  <Image src="/placeholder.svg?height=64&width=64" alt="Popular" fill className="object-cover rounded" />
                                  <div className="absolute inset-0 bg-gradient-to-t from-purple-900/40 to-transparent rounded" />
                                </div>
                                <div>
                                  <h4 className="text-sm font-medium text-white mb-1 hover:text-cyan-400">{post.title}</h4>
                                  <p className="text-xs text-purple-200 hover:text-cyan-400">{post.date}</p>
                                </div>
                              </div>
                            </Link>
                          ))}
                        </div>
                      </CardContent>
                    </Card>

                    <Card className="bg-white/10 backdrop-blur-sm border border-white/20">
                      <CardContent className="p-6">
                        <h3 className="font-semibold text-lg text-white mb-4">{content.sidebar.categories.title}</h3>
                        <div className="space-y-2">
                          {content.sidebar.categories.items.map((cat, i) => (
                            <Link key={i} href={cat.link} className="flex justify-between text-sm text-purple-200 hover:text-cyan-400">
                              <span>{cat.name}</span>
                              <span className="text-cyan-400">({cat.count})</span>
                            </Link>
                          ))}
                        </div>
                      </CardContent>
                    </Card>

                    <Card className="bg-white/10 backdrop-blur-sm border border-white/20">
                      <CardContent className="p-6">
                        <h3 className="font-semibold text-lg text-white mb-4">{content.sidebar.tags.title}</h3>
                        <div className="flex flex-wrap gap-2">
                          {content.sidebar.tags.items.map((tag, i) => (
                            <Link key={i} href={tag.link}>
                              <Badge variant="outline" className="border-cyan-400 text-cyan-400 hover:bg-cyan-400/20">{tag.name}</Badge>
                            </Link>
                          ))}
                        </div>
                      </CardContent>
                    </Card>
                  </div> */}
                </div>
              </CardContent>
            </Card>
          </div>
        </div>
      </div>
      {/* <Footer /> */}
    </div>
  );
}