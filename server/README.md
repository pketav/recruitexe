# Legacy API

The old Express/Mongo backend is not committed here because it contained hardcoded third-party secrets. The original downloaded source remains untouched outside this clean repo. Supabase is the source of truth for the new backend.
